export const trimmingIcon = (image, id, imageSize) => {
    return (
        {
            clipPath: "circle(35%)",
            width: { imageSize },
            height: { imageSize }
        }
    )
}

export default function Home() {
    return (<div></div>)
}